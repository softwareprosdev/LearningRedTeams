{
  "name": "@zdi/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "build": "tsc index.ts --outDir dist --module commonjs --target ES2022 --esModuleInterop --declaration false --sourceMap false --skipLibCheck || true"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^5.9.1",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  }
}
